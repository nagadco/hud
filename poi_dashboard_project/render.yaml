services:
  - type: web
    name: poi-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashbord.py
    plan: free
    envVars:
      - key: PORT
        value: 8050
